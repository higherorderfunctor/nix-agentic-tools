# Alpha mock facet

Alpha demonstrates package, overlay, Home Manager, check, and registry ownership
without owning a library or devenv contribution. Its package asks for
`bravo-tool` through `callPackage`; it never imports Bravo by path.
