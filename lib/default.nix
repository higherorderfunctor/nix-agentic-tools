# Shared library — composes all lib sub-modules.
# Populated as content migrates:
# - fragments.nix (Phase 0.3)
# - mcp.nix + credentials.nix (Phase 3.4)
# - devshell.nix (Phase 2)
{lib}: {
  # Placeholder — real exports added per phase
}
