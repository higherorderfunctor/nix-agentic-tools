# Concepts

Hardened terminology for this project. Free-form one-liners until structure
emerges naturally.

- **Self-assembly** — the architectural target: artifacts emerge from a small
  set of composable patterns flowing through Nix's module system, rather than
  being explicitly hand-wired. The real goal underneath "DRY"; DRY alone is a
  proxy that lets non-composing implementations pass.
