<!-- TODO: remove this file before merging to main -->

# Overnight Work Report — 2026-04-13

## Commits (not pushed — 4 ahead of origin)

| SHA       | Description                                                                                                                                                                                                                                                                                         |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `12f301e` | **devenv auto-regeneration** — instruction files auto-update on shell entry via `files.*` consuming same `dev/generate.nix` import as flake.nix. Added coding-standards overlay to devenv.                                                                                                          |
| `da2af3b` | **CI v4 workflow** (Phase 1 of design, superseded by `5d1645d`)                                                                                                                                                                                                                                     |
| `40bf65a` | **Fragment source comments** — `<!-- Generated by dev/generate.nix -->` after frontmatter, `<!-- Fragment: path/to/source.md -->` before each section. 4 files changed: `lib/fragments.nix`, `dev/generate.nix`, `packages/coding-standards/default.nix`, `packages/stacked-workflows/overlay.nix`. |
| `5d1645d` | **CI redesign** — "Stage, Validate, Push" workflow + plan.md updates (amended into plan commit, sloppy but content correct)                                                                                                                                                                         |
| `73968cb` | **Plan cleanup** — marked completed items, sorted cspell terms                                                                                                                                                                                                                                      |

## Research + Fix: devenv auto-regeneration

### Root cause investigation

The `.claude/rules/*.md`, `.github/instructions/*.md`, `.kiro/steering/*.md`,
`CLAUDE.md`, and `AGENTS.md` files were NOT managed by devenv `files.*`.
Confirmed by checking `.devenv/state/files.json` — zero entries for rules,
instructions, or steering files. Only skills, MCP configs, settings, and
pre-commit config were managed.

These files were produced by explicit devenv tasks
(`generate:instructions:*`) that run `nix build .#instructions-claude`
and `cp` the output. The `ai.instructions` option in the devenv module
was empty — the stacked-workflows devenv module is a no-op stub (only the
HM module contributes instructions).

The nix build path works correctly — `nix build .#instructions-claude`
rebuilds when fragment sources change. But devenv tasks cached the old
store path. Clearing eval cache (`nix-eval-cache.db`) and running
`devenv update` didn't help because the instruction files aren't
tracked by devenv's eval cache.

### Options considered

- **Option A (rejected):** Unconditional `devenv tasks run generate:instructions` in `enterShell`. Adds 2-5s to every shell entry.
- **Option B (implemented):** Wire `dev/generate.nix` into `files.*` directly. Content computed at eval time, materialized as store-backed files.
- **Option C (backup):** Fingerprint check in `enterShell` — hash fragment sources, regen only on change. Sub-second on cache hit.

### Fix implemented (Option B)

Imported `dev/generate.nix` in `devenv.nix` (same import as `flake.nix`,
single source of truth). The `gen` output feeds `files.*` entries that
materialize instruction files on every shell entry. Zero DRY violation.

Added `coding-standards` overlay to devenv (was only in flake before)
because `gen` needs `pkgs.coding-standards.passthru.fragments`.

**Verified:** `.claude/rules/nix-standards.md` shows updated content
with fresh timestamps after `devenv shell` entry. All ecosystem files
materialized correctly.

**Residual:** Files are written but don't appear in devenv's
`files.json` managed files list. Functionally correct — the files exist
with current content and correct permissions. The tracking gap is
cosmetic and likely a devenv version behavior.

## Feature: fragment source comments

Every generated instruction file now includes HTML comments tracing content back to source files:

```markdown
---
paths:
  - "**/*.nix"
---

<!-- Generated by dev/generate.nix -->
<!-- Fragment: dev/fragments/nix-standards/nix-standards.md -->

### Nix

...
```

Changes:

- `lib/fragments.nix`: `mkFragment` gains optional `source` field; `compose` gains optional `generator` field; both inject HTML comments
- `dev/generate.nix`: computes repo-relative paths in `mkDevFragment`
- Content packages: pass `source` to each `mkFrag` call

Zero changes to transformers — comments are part of text body, naturally land after frontmatter.

**Backlog item added:** Fragment source linter to verify all `<!-- Fragment: path -->` comments point to existing files.

## Research: CI cross-platform validation (full)

### Options evaluated

| Option | Approach                                             | Verdict                                 |
| ------ | ---------------------------------------------------- | --------------------------------------- |
| 1      | Export per-target diffs, test each on both platforms | Reject — reimplements ninja DAG in YAML |
| 2      | Run ninja, validate both platforms, then push        | **Interim design** (see below)          |
| 3      | Per-target matrix builds (56 jobs for 28 targets)    | Reject — too expensive                  |
| 4      | Push first, revert if darwin fails                   | Reject — broken state on branch         |
| 5      | Remote darwin builder from ubuntu                    | Reject — needs persistent Mac infra     |

### Interim design: "Stage, Validate, Push" (superseded)

Three-phase workflow: Phase 1 pushes to staging ref, Phase 2 validates
on both platforms, Phase 3 fast-forwards branch. Failure mode: entire
batch held back. Acceptable but user wanted per-dependency granularity.

### Final design: Renovate-style per-dependency PRs

After user feedback, redesigned to match Renovate's model:

1. **Phase 1 (ubuntu):** Run ninja pipeline normally (cherry-picks to
   local branch for build validation)
2. **Phase 2 (ubuntu):** For each update commit between base and HEAD:
   - Create branch `update/<dep-name>` from base
   - Cherry-pick the single commit
   - Force-push (updates existing branch on re-run)
   - Create or update PR against the working branch
3. **ci.yml validates:** PRs trigger ci.yml which builds on both
   platforms (matrix: linux + darwin)
4. **Merge when green:** PRs that pass both platforms can be merged

**Key details:**

- Dep name extracted from commit subject: `chore: update input <name>`
  or `chore(overlays): update <name>` → `update/<name>`
- Non-update commits (treefmt, etc.) are skipped
- Uses `gh pr create` / `gh pr list` for PR management
- Triggers: push to branch + schedule + manual dispatch
- Self-triggering prevention: `github.actor != 'github-actions[bot]'`

**One script change:** `MERGE_LOCK` in `update-common.sh` now respects
env override for CI where `/run/user/$UID/` may not exist.

## Research: Nix doc comment tooling (full)

Also saved to memory at `reference_nix_doc_tooling.md`.

**User's goal:** Extract doc comments from `.nix` source files and feed them into the fragment pipeline as first-class fragments (instead of separate markdown files). Single source of truth — the code IS the documentation.

### RFC 145 doc-comment format (the standard)

```nix
{
  /** This function adds two numbers
    # Type
    add :: Number -> Number -> Number
  */
  add = a: b: a + b;
}
```

`/**` (double asterisk) distinguishes doc-comments from regular `/* */`. Content is CommonMark. Accepted RFC, partially implemented:

- `nix repl :doc` renders them (Nix 2.24+, July 2024)
- nixd LSP shows them on hover
- nixdoc CLI extracts them to markdown

### Tools

**nixdoc** (nix-community/nixdoc): Rust CLI using `rnix` parser. Extracts `/**` comments from `.nix` files → CommonMark markdown. THE tool used by nixpkgs to generate lib reference docs. Works on custom libraries. Invocation:

```bash
nixdoc -c "$name" -d "lib.$name: $description" -f "lib/$name.nix" > "$out/$name.md"
```

**Limitation:** Expects flat attribute sets. Cannot follow dynamic attr generation or deeply nested structures. Our overlays are functions `{ inputs, final, ... }: let ... in derivation` — nixdoc can't parse these directly.

**nix-doc** (lf-/nix-doc): Interactive search + REPL plugin. Three components:

1. CLI search (`nix-doc search <regex> [dir]`) — grep for functions with docs
2. REPL plugin (`builtins.doc f`) — show docs for any lambda
3. Tags generator — vim ctags for Nix files

Complements nixdoc (interactive reading vs static generation).

**docnix** (nix-community/docnix): Ambitious unified doc gen project by @hsjobeki. Has `codemod` for auto-migrating old comments to RFC 145. Includes custom builtins (`unsafeGetAttrDoc`, `unsafeGetLambdaDoc`). NOT production-ready.

**nixd LSP** (nix-community/nixd): Shows RFC 145 doc-comments on hover. Uses real Nix evaluator (not rnix). We already have this in our devenv (nixd in packages).

### How nixpkgs does it

Path: source `/**` comments → `nixdoc` CLI → CommonMark → nixos-render-docs → HTML manual.

The `lib-function-docs.nix` in nixpkgs loops over `libsets = [{name, description}]`, runs `nixdoc` per file, includes output in the manual. `locations.json` maps functions to GitHub source links.

### Extraction approach for our fragment pipeline

To use doc comments as fragment sources:

1. Add `/**` doc-comments to overlay `.nix` files, lib functions, etc.
2. Extract at build time via one of:
   - **nixdoc** on files restructured as flat attrsets (wrapper file)
   - **Custom Nix-native extractor** using `builtins.readFile` + string parsing (no external tool, works on any file structure)
   - **docnix** when it stabilizes
3. Feed extracted markdown into `mkFragment` with `source` pointing at the `.nix` file
4. `compose` and transforms work as normal — the extracted doc becomes a fragment like any other

**Challenge:** Our overlays aren't flat attrsets. Options:

- Write a thin wrapper `.nix` that re-exports the overlay's doc-worthy attributes as an attrset for nixdoc
- Write a custom Nix string parser (`builtins.match` on `readFile` output) to extract `/**` blocks
- Use `nix eval` with `builtins.unsafeGetAttrDoc` (docnix approach, not stable)

### NixOS module option documentation

Module options use `mkOption { description = "..."; }`. Rendered by:

- `nixosOptionsDoc` → `options.json` → NuschtOS Search (we already do this)
- `nixos-render-docs` → HTML manual (nixpkgs only)

We already have this working via `lib/options-doc.nix` + NuschtOS `mkMultiSearch`. The `description` field is the highest-ROI documentation surface for modules.

### Practical tiers (effort vs value)

| Tier | Action                                                | Effort | Value                                                   |
| ---- | ----------------------------------------------------- | ------ | ------------------------------------------------------- |
| 1    | Improve `description` fields on all module options    | Low    | High — already renders in NuschtOS                      |
| 2    | Add `/**` doc-comments to lib/ functions              | Low    | Medium — enables `:doc` and nixd hover                  |
| 3    | Hand-written mdbook API docs chapter                  | Medium | High — fills gap between options browser and onboarding |
| 4    | Wire nixdoc into build for auto-generated markdown    | Medium | Medium — only worth it if lib surface grows large       |
| 5    | Custom extractor for overlay doc-comments → fragments | High   | High — single source of truth for package docs          |

### Not recommended

- **nixos-render-docs** for custom projects — tightly coupled to nixpkgs
- **nmd** (NixOS Module Documentation) — stale, NuschtOS search covers it
- **docnix** — watch but don't depend on yet

## Backlog cleanup

Marked as completed in `docs/plan.md`:

- nvfetcher cleanup section (55→1 references)
- All update pipeline improvements (ninja DAG, worktrees, flock, version tracking, smoke tests, unit tests)
- Instruction regeneration (now auto via devenv files.\*)
- Architecture fragment updates (buddy-activation, wrapper-chain)
- `cspell/project-terms.txt` `endgroup` sorted alphabetically

## CI redesign v2: Renovate-style per-dependency PRs

After user feedback, redesigned from "Stage, Validate, Push" to
Renovate-style per-dependency PRs:

1. **Phase 1 (ubuntu):** Run ninja pipeline normally (cherry-picks to local branch)
2. **Phase 2 (ubuntu):** For each update commit, create a branch `update/<name>`
   and open/update a PR against the working branch
3. **ci.yml validates:** PRs trigger ci.yml which builds on both platforms
4. **Merge when green:** PRs that pass both platforms can be merged

**Key advantage:** Each dependency is independently validated on both
platforms before merging. Failed darwin builds only hold back that
specific dependency, not the entire batch.

**Existing branches are force-updated** on re-run (rebase on current
tip, redo update). Like Renovate's behavior.

**Commit:** `0b541f8` — not pushed yet.

## Gaps / decisions needed

1. **GITHUB_TOKEN limitation (CRITICAL):** PRs created by `github-actions[bot]`
   using the default `GITHUB_TOKEN` won't trigger ci.yml's `pull_request`
   event. This is a GitHub security feature. Options:
   - Use a PAT (Personal Access Token) stored as a secret
   - Use a GitHub App token
   - User manually triggers CI on each PR (defeats automation purpose)
   - Use `workflow_dispatch` on ci.yml triggered by update workflow
     **This needs user input before pushing.**

2. **`on: push` kept** — CI triggers on push to the branch, as user requested.
   This means each push runs the update pipeline, which creates PRs.

3. **devenv `files.json` tracking** — instruction files are materialized
   correctly but don't appear in devenv's managed files list. Functionally
   correct (files are written), tracking gap is cosmetic.

4. **Nix doc comments → fragments** — future task, research saved to memory.
   User wants to review research before implementation.

5. **Commit hygiene** — several amended/mixed commits in the not-yet-pushed
   history. Will be re-chunked for main merge.

## Future: code→markdown generation + non-fragment files

**Vision:** Most markdown should be generated from fragments or
inferred from source code. E.g., `overlays/README.md` package table
should be computed from overlay metadata at eval time, not hand-maintained.

**Non-fragment SOURCE markdown files in the repo:**

Candidates for conversion to fragments:

- `overlays/README.md` — package table should be generated from overlay metadata
- `CONTRIBUTING.md` — partially generated but has hardcoded sections
- `README.md` — could be generated from nix eval data
- `.github/copilot-instructions.md` — generated but some SOURCE files exist under `.github/instructions/`

Already fragments (good):

- `dev/fragments/**/*.md` (18 files) — dev-only instruction fragments
- `packages/coding-standards/fragments/*.md` (5 files) — published fragments
- `packages/stacked-workflows/fragments/*.md` (1 file) — routing table
- `packages/claude-code/docs/*.md` (2 files) — buddy/wrapper docs

Documentation (not fragment candidates):

- `dev/docs/**/*.md` (11 files) — mdbook authored prose
- `dev/notes/*.md` (4 files) — historical dev notes
- `dev/references/*.md` (2 files) — dev reference docs
- `dev/skills/**/*.md` (20+ files) — skill definitions
- `devshell/docs-site/pages/*.md` (7 files) — doc site static pages
- `docs/superpowers/**/*.md` (4 files) — historical specs/plans
- `packages/stacked-workflows/skills/**/*.md` (20+ files) — published skills
- `packages/stacked-workflows/references/*.md` (5 files) — published references

Some `.github/instructions/*.md` files are tracked SOURCE — they must stay
committed because Copilot auto-review reads from the repo. devenv generates
them on shell entry; the commit hook guards staleness.

## Open ideas from user (captured for next session)

1. **Ecosystem-specific instructions in fragment comments** — the Claude
   transform could inject a note like `<!-- This file is generated from
fragments. Edit the source, not this file. -->` that only appears in
   Claude rule files. Could tell Claude to `git add` these files. Research:
   how do other projects handle generated-but-committed file instructions?

2. **Code to markdown flows** — overlays/README.md package table should be
   inferred from overlay metadata at eval time (reflection pattern).
   String-interpolate into a fragment, fan out to README.

3. **Doc comments as fragments** — RFC 145 `/**` comments in `.nix` files
   extracted into the fragment pipeline. See Nix doc tooling research above.

4. **devenv generates + commit hook ALSO generates** — both trigger same
   generation logic (DRY). devenv shell entry produces files; commit hook
   also regenerates and re-stages. Catches edits to fragments without
   re-entering devenv. Implementation note: hook would need `nix build`
   which adds ~2-5s to every commit. Consider caching/fingerprinting to
   skip when fragments haven't changed. Currently devenv files.\* handles
   generation on shell entry; treefmt-restage handles re-staging.

---

## Addendum: 2026-04-13 daytime session

### CI workflow first run results

The update workflow (`update.yml`) ran on push. Setup steps all passed
(App token, checkout, nix, cachix, tools). The ninja pipeline ran but
had errors. PR creation step failed.

**Fixes applied:**

- `nix profile install` → `nix profile add` (deprecated alias warning)
- Heredoc in YAML broke GHA parser → replaced with `--body-file`
- `git branch --list` output has `+` markers for worktree branches →
  strip with `tr -d ' *+'`
- `NIX_PATH=nixpkgs=flake:nixpkgs` added (nix-update uses `import <nixpkgs>`)

**After fixes:** Workflow YAML parsed correctly (name "Update" shown).
Pipeline ran. PR creation step still failed due to the `+` marker
issue (fixed in second push).

### Known bug: two-phase commit eval cache staleness (NOT FIXED)

**Severity:** Affects packages with upstream changes. No impact when
packages are already at latest.

**Symptoms:** `error: path '/nix/store/...-source.drv' is not valid`
during nix-update evaluation in worktrees.

**Root cause analysis:**

The update flow for main-tracking packages:

1. `git ls-remote` gets new rev
2. `sed` replaces rev in the `.nix` file
3. `nix flake prefetch` gets new src hash
4. `sed` replaces hash in the `.nix` file
5. `git commit --no-verify` (wip commit with rev + src hash)
6. `nix-update --version skip` runs to update dep hashes (cargo, pnpm, vendor, etc.)

Step 6 fails because nix-update evaluates the package via:

```
nix-instantiate --eval ... --argstr flakeImportPath /nix/store/<hash>-source ...
```

The `flakeImportPath` is a nix store copy of the worktree's flake source.
After the wip commit (step 5), the store copy SHOULD reflect the new state.
But nix's eval cache may serve a stale store path that references the OLD
src hash derivation. The old derivation's output path doesn't exist in the
store (it was for the old rev), causing the `path is not valid` error.

**Why it works sometimes:** When there are no upstream changes (rev
unchanged), steps 1-5 are skipped entirely. nix-update evaluates against
the unchanged flake source, which is fully cached and valid.

**Why it worked before the overnight session:** Before the two-phase
commit was added, nix-update ran with `--commit` (which it handled
internally). The two-phase approach was added to fix the pre-commit hook
crash in worktrees (worktrees lack `.pre-commit-config.yaml`). The fix
introduced the eval cache staleness as a side effect.

**Possible fixes (not implemented, needs HITL):**

1. **Delete the worktree's eval cache before nix-update:**
   `rm -f ~/.cache/nix/eval-cache-v6/*.sqlite` — nuclear, affects all
   evaluations, not just this worktree.

2. **Use `--no-eval-cache` flag on nix-update's internal nix calls:**
   nix-update doesn't expose this. Would need a patch or wrapper.

3. **Force nix to re-evaluate the flake source:**
   After the wip commit, run `nix flake metadata --json .` in the worktree
   to force the flake source to be re-imported into the store. This should
   populate the new store path before nix-update tries to use it.

4. **Revert to single-commit approach with --no-verify:**
   The pre-commit hook issue was the original reason for two-phase. But
   we now symlink `.pre-commit-config.yaml` into worktrees. We could go
   back to a single commit approach and let hooks run (they have the
   config now). This would avoid the eval cache issue entirely.

5. **Skip nix-update for dep hashes when rev changed:**
   If the rev+hash was bumped, just commit that. Dep hash mismatches
   will be caught by the build step (local) or CI (remote). The build
   failure gets reported as HELD BACK with the version info. Next run,
   if dep hashes need updating, nix-update can handle it from a clean
   committed state.

**Recommendation:** Option 4 (revert to single commit) is simplest.
The pre-commit config symlink was added specifically to enable hooks in
worktrees. The two-phase approach was a workaround for missing hooks —
now that hooks work, the workaround is unnecessary. But this needs user
review since the original two-phase design was explicitly approved.

### OOM during local pipeline run

**Symptoms:** System OOM killer invoked during `ninja -j4` local run.
Killed Slack (pid 2385923) and Chromium (pid 2313660).

**Root cause:** Not a regression — the pipeline always used `-j4` for
parallel nix evaluations. Each flake eval uses 1-2GB. Four concurrent
evals + desktop apps exhausted system memory.

**Contributing factors from overnight work:**

- any-buddy converted from `dontBuild` to full pnpm build + 203 tests
- kiro-gateway gained 1413 pytest tests
- These are the first builds of these new derivations (not cached)
- The final `nix-fast-build` step evaluates all packages

**Mitigation:** Use `-j2` or `-j1` for reduced memory pressure. Or
close heavy apps before running. After the first successful build,
derivations are cached and subsequent runs use much less memory.

### CI workflow redesign (implemented, pushed)

Redesigned from "Stage, Validate, Push" to Renovate-style per-dependency
PRs. Key changes:

**Scripts (`update-common.sh`):**

- `UPDATE_CI=1` env var enables CI mode
- `setup_worktree`: always uses named branch `update/<name>` (unified
  with local — no more detached HEAD)
- `run_build`: wrapper that skips in CI mode
- `merge_to_branch`: skips cherry-pick in CI mode
- `update-init.sh`: cleans up stale `update/*` branches, strips `+`

**Workflow (`update.yml`):**

- GitHub App token (`nix-agentic-tools-bot`) for PR creation
- Triggers: push + manual dispatch (no schedule)
- Runs ninja with `UPDATE_CI=1` (no builds, no cherry-picks)
- After ninja: iterates `update/*` branches, pushes each, opens PRs
- Self-trigger guard: `github.actor != 'nix-agentic-tools-bot[bot]'`

**GITHUB_TOKEN gap resolved:** Using GitHub App token. PRs created by
the app DO trigger `pull_request` events in ci.yml (confirmed from
GitHub docs: only `GITHUB_TOKEN` has the recursive prevention, not
app installation tokens).

### GitHub instruction files tracking

Fixed: removed `.github/instructions/` and `.github/copilot-instructions.md`
from `.gitignore`. These are generated by devenv but must be committed
for Copilot auto-review. 8 previously-ignored files now tracked.

### treefmt drift (devenv shell vs commit hook)

`devenv shell` entry runs treefmt on ALL files. Pre-commit hook runs
treefmt only on staged files. This causes drift when shfmt settings
change (files committed with old formatting get reformatted on shell
entry). No clean fix found — formatting all files in the hook would
stage unrelated changes. Accepted as a known quirk. Workaround: commit
the reformatted file when it appears.

### CI eval failure on update PRs (NOT FIXED — needs design decision)

**Severity:** Blocks ALL update PRs from passing CI.

**Symptoms:** Every update PR fails ci.yml at "Check flake evaluation"
with `error: path '...-source.drv' is not valid`. Fails on both
x86_64-linux and aarch64-darwin.

**Root cause: Import From Derivation (IFD) during eval.**

Our overlays compute version at eval time from the fetched source:
```nix
version = vu.mkVersion {
  upstream = vu.readPackageJsonVersion "${src}/package.json";
  inherit rev;
};
```

`builtins.readFile "${src}/package.json"` is IFD — nix must realize
(fetch) the `fetchFromGitHub` derivation before evaluation can continue.
On the CI runner evaluating a PR branch:
1. PR has new rev + new src hash
2. CI runner does `nix flake check` or `nix eval`
3. Nix tries to realize `fetchFromGitHub { rev = "newrev"; hash = "newhash"; }`
4. The source derivation (`.drv`) doesn't exist in the runner's store
5. It's not in cachix (update pipeline skipped builds in CI mode)
6. Nix errors with `path is not valid` instead of fetching from GitHub

**Why it worked locally:** Local machine has sources cached from
prior builds. CI runners start fresh.

**Fix options:**

1. **Stop computing version from source at eval time.** Use literal
   version strings written by the update script. Eliminates IFD. Loses
   auto-computed version feature. Update script already has version info
   from nix-update output.

2. **Prefetch sources before pushing PR branches.** Add a step to the
   update workflow that runs `nix build .#<pkg>.src --no-link` for each
   updated package. Fetches the source, cachix daemon pushes it. CI
   runner substitutes during eval. One extra GitHub tarball fetch per
   updated package — fast.

3. **Run `nix build .#<pkg>.src` in the update script** (even in CI
   mode). Similar to option 2 but inside the script instead of workflow.

4. **Change ci.yml to allow IFD / use `--impure`.** Weakens eval purity.
   Not recommended.

**Recommendation:** Option 2 or 3. Prefetch the source so it's in
cachix for the CI runner. Minimal change, doesn't alter architecture.
Option 1 is cleaner long-term but larger refactor of the version
computation pattern.

### Docs workflow scope fix

Fixed: `branches: ["**"]` → `[main, refactor/ai-factory-architecture]`.
Was triggering docs builds on every `update/*` branch push, causing
Pages deployment races and unnecessary builds.

### Eval cache fix (single commit + amend)

Implemented option 4 from the analysis: replaced two-phase wip commits
with single real commit + amend for dep hashes. Hooks run on the first
commit (pre-commit config symlinked). Dep hash changes amend into same
commit. Eliminates the eval cache staleness issue for local runs.
CI eval failure is a separate issue (IFD, see above).
