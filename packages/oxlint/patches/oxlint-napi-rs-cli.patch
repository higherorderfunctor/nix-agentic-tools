diff --git a/patches/@napi-rs__cli@3.10.1.patch b/patches/@napi-rs__cli@3.10.1.patch
new file mode 100644
index 0000000..fa44a41
--- /dev/null
+++ b/patches/@napi-rs__cli@3.10.1.patch
@@ -0,0 +1,90 @@
+diff --git a/dist/cli.js b/dist/cli.js
+index 49121847bea8b8abb1f8894287ac596f9f680b7f..76033c166a707827f7a8bf6f381e96da0a7a7493 100755
+--- a/dist/cli.js
++++ b/dist/cli.js
+@@ -2690,18 +2690,22 @@ async function readPosixProcessIncarnation(pid) {
+ }
+ function executeProcessIncarnationCommand(command, args, env = process.env, timeout = processIncarnationCommandTimeout) {
+ 	return new Promise((resolveCommand) => {
+-		execFile(command, args, {
+-			encoding: "utf8",
+-			env,
+-			timeout,
+-			windowsHide: true
+-		}, (error, stdout) => {
+-			if (error) {
+-				resolveCommand(null);
+-				return;
+-			}
+-			resolveCommand(stdout.trim() || null);
+-		});
++		try {
++			execFile(command, args, {
++				encoding: "utf8",
++				env,
++				timeout,
++				windowsHide: true
++			}, (error, stdout) => {
++				if (error) {
++					resolveCommand(null);
++					return;
++				}
++				resolveCommand(stdout.trim() || null);
++			});
++		} catch {
++			resolveCommand(null);
++		}
+ 	});
+ }
+ function assertReconciliationLockAcquisitionTimeRemaining(acquisitionDeadline, key) {
+diff --git a/src/utils/misc.ts b/src/utils/misc.ts
+index 0b10aa463a99072c89e7e34acfe4ceb65a55877a..0e6f7aa1599932ac6169f0bb4c7d5d68c6941b81 100644
+--- a/src/utils/misc.ts
++++ b/src/utils/misc.ts
+@@ -3962,24 +3962,28 @@ function executeProcessIncarnationCommand(
+   timeout: number = processIncarnationCommandTimeout,
+ ): Promise<string | null> {
+   return new Promise((resolveCommand) => {
+-    execFile(
+-      command,
+-      args,
+-      {
+-        encoding: 'utf8',
+-        env,
+-        timeout,
+-        windowsHide: true,
+-      },
+-      (error, stdout) => {
+-        if (error) {
+-          resolveCommand(null)
+-          return
+-        }
+-        const output = stdout.trim()
+-        resolveCommand(output || null)
+-      },
+-    )
++    try {
++      execFile(
++        command,
++        args,
++        {
++          encoding: 'utf8',
++          env,
++          timeout,
++          windowsHide: true,
++        },
++        (error, stdout) => {
++          if (error) {
++            resolveCommand(null)
++            return
++          }
++          const output = stdout.trim()
++          resolveCommand(output || null)
++        },
++      )
++    } catch {
++      resolveCommand(null)
++    }
+   })
+ }
+ 
