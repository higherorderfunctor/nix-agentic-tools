# Home-Manager Options Reference

Auto-generated from module definitions via `nixosOptionsDoc`. See
[Home-Manager Setup](../getting-started/home-manager.md) for
installation and quickstart.

## Import

```nix
imports = [inputs.nix-agentic-tools.homeManagerModules.default];
```

The single module includes AI CLI config (Claude, Copilot, Kiro),
MCP server management, and stacked-workflow presets.

## Options
