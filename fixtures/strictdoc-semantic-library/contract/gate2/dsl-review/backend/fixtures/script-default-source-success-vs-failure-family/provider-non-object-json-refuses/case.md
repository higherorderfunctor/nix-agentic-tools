Variant of this family; read ../case.md for the situation, the change and the
citations. Binding: ["sh","-c","echo 42"] with timeoutSeconds 10 — well-formed
JSON of the wrong type, the catalogue's "wrong type" mode. Expected:
baseline-preserved error, causes ["model:reference/input:baseline"], one
non-leaf finding with code `execution`; envelope baseline null; envelope error.
Why: stdout must be one JSON snapshot OBJECT (contract.md:520-522), and
malformed shape is an execution error alongside malformed JSON
(contract.md:523-525).

provenance: DFT04
