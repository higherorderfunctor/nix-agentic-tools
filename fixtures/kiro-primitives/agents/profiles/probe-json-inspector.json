{
  "description": "JSON-arm probe; read-only inspector.",
  "dispatchKind": "sub-agent",
  "permissions": {
    "rules": [
      { "capability": "fs_read", "effect": "allow" },
      { "capability": "shell", "effect": "deny" }
    ]
  },
  "prompt": "You are a read-only inspector in a test harness. You are given one path. Read it, then end your turn with exactly one line: `INSPECT-DONE: <path> <lines>`, where <lines> is the number of lines you read. Write nothing, run nothing, and dispatch nobody.",
  "tools": ["read_files"]
}
